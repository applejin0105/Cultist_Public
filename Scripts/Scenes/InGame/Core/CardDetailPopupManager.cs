using UnityEngine;

namespace Scenes.InGame.Core
{
    public class CardDetailPopupManager : MonoBehaviour
    {
        // 카드의 원본 데이터(CardCatalog.Instance.Get(cardId))를 넘겨받아 화면 중앙에 크게 띄우고, 배경(Dimmed 패널) 클릭 시 창이 닫히는 기능을 구현
    }
}